# Restored from retired hypr/nvidia.conf (was only reaching compositor children via HyDE env.lua).
# Covers systemd user services and transient app scopes (brave-origin runs as app-*.scope).
LIBVA_DRIVER_NAME=nvidia
__GLX_VENDOR_LIBRARY_NAME=nvidia
GBM_BACKEND=nvidia-drm
